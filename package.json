{
  "name": "portfolio-html-tailwind",
  "version": "1.0.0",
  "description": "Portfolio avec HTML et TailwindCSS",
  "main": "index.html",
  "scripts": {
    "build": "pnpm exec tailwindcss -i ./theme-cssglobale.css -o ./dist/output.css --minify",
    "dev": "pnpm exec tailwindcss -i ./theme-cssglobale.css -o ./dist/output.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
