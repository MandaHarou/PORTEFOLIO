{
  "type": "module",
  "devDependencies": {
    "eslint": "^10.8.0",
    "http-server": "^14.1.1"
  },
  "scripts": {
    "dev": "http-server .",
    "lint": "eslint src",
    "lint:fix": "eslint src --fix",
    "format": "prettier . --write",
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "echo \"No build required\""
  },
  "dependencies": {
    "lint": "^1.2.2"
  }
}
